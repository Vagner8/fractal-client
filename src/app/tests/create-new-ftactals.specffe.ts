// import { User } from './user';
// import { configureTestingModule } from './testing-module';
// import { HarnessLoader } from '@angular/cdk/testing';
// import { ComponentFixture } from '@angular/core/testing';
// import { AppComponent } from 'app/app.component';
// import { MatCardHarness } from '@angular/material/card/testing';
// import { MatButtonHarness } from '@angular/material/button/testing';
// import { INDICATORS, MODIFIERS, ENV } from '@constants';
// import { HttpTestingController } from '@angular/common/http/testing';
// import { MatInputHarness } from '@angular/material/input/testing';
// import { MatFormFieldHarness } from '@angular/material/form-field/testing';
// import { IFractalDto } from '@types';
// import { FractalDto } from '@utils';

describe('Should create new fractal(s) #', () => {
  // let user: User;
  // let fixture: ComponentFixture<AppComponent>;
  // let loader: HarnessLoader;
  // let httpTesting: HttpTestingController;

  // beforeEach(async () => {
  //   ({ user, loader, fixture, httpTesting } = await configureTestingModule({
  //     component: AppComponent,
  //   }));
  //   await user.goesToStartPage();
  // });

  describe('If the New modifier was touched on the table page #', () => {
    // beforeEach(async () => {
    //   await user.touchedManager();
    //   await user.touchedCollection('EmptyCollection');
    //   await user.touchedManager();
    //   await user.touchedModifier('New');
    //   await user.heldModifier('Save');
    // });
    // describe('If the selected parent fractal does not have children #', () => {
    //   it('If the new fractal has no dirty controls', async () => {
    //     const fractalReq = httpTesting.expectOne(ENV.FRACTAL_API);
    //     const [newFractalDto] = fractalReq.request.body as [FractalDto];
    //     expect(newFractalDto.controls[INDICATORS.CURSOR].data).toBe('1');
    //   });
    // });
  });
});
